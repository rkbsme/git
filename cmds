Clone a project from GitHub
#git clone https://github.com/vaadin/addressbook.git

Initialize a local Git repository for the project
#git init
Add all files to local repo
# git add .
# git add -A
# git commit -a -m "Cloned and uploaded address book app"



Create New Repo on Git Hub.com

#git config --global user.email "ramakrishna.bejjanki@gmail.com"
#git config --global user.name "Ramakrishna Bejjanki"

#git remote set-url origin https://github.com/rkbsme/ansible.git
#git remote add origin https://github.com/rkbsme/ansible.git

#git remote -v
#git push -u origin main

------Store username and pwd
#git config --global credential.helper store

-------------Ignore files----

List all Ignored files.
$git check-ignore *

.gitignore file
lib/* --All directoty
!lib/file1 -- except this file

$ git help clean
$ git clean -ndX

git-clean - Remove untracked files from the working tree
-n, --dry-run - Don't actually remove anything, just show what would be done.
-d - Remove untracked directories in addition to untracked files.
-X - Remove only files ignored by Git.



